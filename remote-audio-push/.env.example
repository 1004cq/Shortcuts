PORT=3002
HOST=0.0.0.0
# Admin console only — can emit play/stop
ADMIN_TOKEN=change-me-admin-long-random
# iPhone receivers — join only (defaults to ADMIN_TOKEN if empty)
RECEIVER_TOKEN=
# Backward compatible alias for ADMIN_TOKEN
# AUTH_TOKEN=
PUBLIC_URL=https://cq.imim.chat/realtime
CORS_ORIGIN=https://cq.imim.chat
MAX_AUDIO_URL_LENGTH=2048
